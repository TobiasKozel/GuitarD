#pragma once

namespace guitard {
  template <typename T>
  class RingBuffer {
  public:
    void setSize(size_t size) {
      
    }

    void get(T* out, size_t elements) {
      
    }

    void add(T* in, size_t elements) {
      
    }

    size_t inBuffer() const {
      return 0;
    }
  };
}